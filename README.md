# VibeSpace Final - Next.js Project

Dự án Front-End được xây dựng bằng framework Next.js (App Router) kết hợp với thư viện Ant Design (Antd).

## 🚀 Hướng dẫn cài đặt và Chạy dự án

### 1. Khởi tạo dự án (Khi chuyển sang máy mới)
Nếu bạn vừa tải dự án về hoặc chuyển sang máy tính khác (ví dụ: ngoài quán net), hãy mở Terminal (`Command Prompt` hoặc `Git Bash`) tại thư mục gốc của dự án và chạy lệnh sau để cài đặt các thư viện cần thiết:

```bash
npm install
